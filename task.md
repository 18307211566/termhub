未完成：
1、新建会话时，默认账号密码都改成admin，现在默认密码还是pass，需要修改
2、现在新建会话编辑会话参数时，有很多参数都只有一个输入框，没有显示参数名称的Label，这样不利于用户理解，优化一下
3、http代理不需要再界面上显示这些信息：{SSH 接入：ssh admin@<本机IP> -p 2222 用户: admin · 密码: ***}
4、如果打开软件后，会话如果是上次创建的，运行时间会一直显示0S
5、在界面上踢下联时，报错：invalid args `clientId` for command `kick_client`: command kick_client missing required key clientId
6、上游是本地Shell时，SSH连接不成功，输入ssh admin@127.0.0.1 -p 2223后卡在不动
7、每次打开软件会自动创建echo的loopback会话，改掉，并且删除上游选项中的loopback
8、现在的HTTP代理（端口转发）实际上就是端口转发，把名称直接改成端口转发，并且增加TCP与UDP的选项，可以选这两个种，并且默认选项为TCP



已完成：
1、现在所有的会话都是对应的本机的一个端口上，这样不好，修改为每个会话开启一个本机端口，并在创建或设置中可以设置 
2、新建会话时，会话名是SSH的登录名，这不合理，会话名就是会话名，SSH的用户名应该分开，并且将默认用户名与密码都为admin
3、当停止会话时，整个会话像是被删掉了，在界面里找不到了，这不对，停止会话应该只把会话列表中的绿点编程灰色，可以再次打开，打开后又变成绿点
4、现在编辑某个会话时，只能设置密码，需要改成全部参数都可以编辑
5、现在选择串口时，只能手动填写对应COM口名字，应该扫描有那些串口，并显示未下拉框，让用户选择
6、现在会话的运行时间一直显示为“运行 0S”
